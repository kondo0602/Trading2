<% provide(:title, 'お気に入りした商品一覧') %>
  <h1>お気に入りした商品一覧</h1>

  <aside class="col-md-4">
    <%= render 'shared/user_sidebar' %>
  </aside>

  <div class="col-md-8">

    <ul class="itemindex">
      <% if current_user.liked_items.any? %>
        <%= will_paginate @items, :previous_label => ' &lt 前へ', :next_label => '次へ &gt' %>

        <%= render @items %>

        <%= will_paginate @items, :previous_label => ' &lt 前へ', :next_label => '次へ &gt' %>
      <% else %>
        <p>まだお気に入りしている商品はありません。</p>
      <% end %>
    </ul>